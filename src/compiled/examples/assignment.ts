/**
 *
 * This file is auto-generated. Do not edit manually: changes may be erased.
 * Generated by Aqua compiler: https://github.com/fluencelabs/aqua/.
 * If you find any bugs, please write an issue on GitHub: https://github.com/fluencelabs/aqua/issues
 * Aqua version: 0.2.0-SNAPSHOT
 *
 */
import { FluencePeer } from '@fluencelabs/fluence';
import {
    ResultCodes,
    RequestFlow,
    RequestFlowBuilder,
    CallParams,
} from '@fluencelabs/fluence/dist/internal/compilerSupport/v1';

// Services

export function registerOpHa(service: {
    array: (a: string, b: string, callParams: CallParams<'a' | 'b'>) => Promise<string[]>;
    identity: (a: string, callParams: CallParams<'a'>) => Promise<string>;
}): void;
export function registerOpHa(
    serviceId: string,
    service: {
        array: (a: string, b: string, callParams: CallParams<'a' | 'b'>) => Promise<string[]>;
        identity: (a: string, callParams: CallParams<'a'>) => Promise<string>;
    },
): void;
export function registerOpHa(
    peer: FluencePeer,
    service: {
        array: (a: string, b: string, callParams: CallParams<'a' | 'b'>) => Promise<string[]>;
        identity: (a: string, callParams: CallParams<'a'>) => Promise<string>;
    },
): void;
export function registerOpHa(
    peer: FluencePeer,
    serviceId: string,
    service: {
        array: (a: string, b: string, callParams: CallParams<'a' | 'b'>) => Promise<string[]>;
        identity: (a: string, callParams: CallParams<'a'>) => Promise<string>;
    },
): void;
export function registerOpHa(...args) {
    let peer: FluencePeer;
    let serviceId;
    let service;
    if (args[0] instanceof FluencePeer) {
        peer = args[0];
    } else {
        peer = FluencePeer.default;
    }

    if (typeof args[0] === 'string') {
        serviceId = args[0];
    } else if (typeof args[1] === 'string') {
        serviceId = args[1];
    } else {
        serviceId = 'op';
    }

    if (!(args[0] instanceof FluencePeer) && typeof args[0] === 'object') {
        service = args[0];
    } else if (typeof args[1] === 'object') {
        service = args[1];
    } else {
        service = args[2];
    }

    peer.callServiceHandler.use(async (req, resp, next) => {
        if (req.serviceId !== serviceId) {
            await next();
            return;
        }

        if (req.fnName === 'array') {
            const callParams = {
                ...req.particleContext,
                tetraplets: {
                    a: req.tetraplets[0],
                    b: req.tetraplets[1],
                },
            };
            resp.retCode = ResultCodes.success;
            resp.result = await service.array(req.args[0], req.args[1], callParams);
        }

        if (req.fnName === 'identity') {
            const callParams = {
                ...req.particleContext,
                tetraplets: {
                    a: req.tetraplets[0],
                },
            };
            resp.retCode = ResultCodes.success;
            resp.result = await service.identity(req.args[0], callParams);
        }

        await next();
    });
}

// Functions

export async function doSmth(arg: { value: string }, config?: { ttl?: number }): Promise<string[]>;
export async function doSmth(peer: FluencePeer, arg: { value: string }, config?: { ttl?: number }): Promise<string[]>;
export async function doSmth(...args) {
    let peer: FluencePeer;
    let arg;
    let config;
    if (args[0] instanceof FluencePeer) {
        peer = args[0];
        arg = args[1];
        config = args[2];
    } else {
        peer = FluencePeer.default;
        arg = args[0];
        config = args[1];
    }

    let request: RequestFlow;
    const promise = new Promise<string[]>((resolve, reject) => {
        const r = new RequestFlowBuilder()
            .disableInjections()
            .withRawScript(
                `
     (xor
 (seq
  (seq
   (seq
    (seq
     (call %init_peer_id% ("getDataSrv" "-relay-") [] -relay-)
     (call %init_peer_id% ("getDataSrv" "arg") [] arg)
    )
    (call %init_peer_id% ("op" "identity") [arg.$.value!] a)
   )
   (call %init_peer_id% ("op" "array") [a "hello"] res)
  )
  (xor
   (call %init_peer_id% ("callbackSrv" "response") [res])
   (call %init_peer_id% ("errorHandlingSrv" "error") [%last_error% 1])
  )
 )
 (call %init_peer_id% ("errorHandlingSrv" "error") [%last_error% 2])
)

                 `,
            )
            .configHandler((h) => {
                h.on('getDataSrv', '-relay-', async () => {
                    return peer.connectionInfo.connectedRelays[0];
                });
                h.on('getDataSrv', 'arg', async () => {
                    return arg;
                });
                h.onEvent('callbackSrv', 'response', async (args) => {
                    const [res] = args;
                    resolve(res);
                });

                h.onEvent('errorHandlingSrv', 'error', async (args) => {
                    const [err] = args;
                    reject(err);
                });
            })
            .handleScriptError(reject)
            .handleTimeout(() => {
                reject('Request timed out for doSmth');
            });
        if (config && config.ttl) {
            r.withTTL(config.ttl);
        }
        request = r.build();
    });
    await peer.initiateFlow(request!);
    return promise;
}
