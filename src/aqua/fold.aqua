import "println.aqua"

-- showcases `for` instruction that compiles to `fold` in AIR
func iterateAndPrint(strings: []string):
    for s <- strings:
        print(s)

func iterateAndPrintParallel(strings: []string):
    for s <- strings par:
        print(s)
