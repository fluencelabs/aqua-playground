import "builtin.aqua"

service ParAbb("parabb"):
    call: -> string

func parFunc( node: string, c: ExternalAddresses -> () ):
    y <- ParAbb.call()
    par on node:
        t <- Peer.identify()
        c(t)
    par x <- ParAbb.call()
