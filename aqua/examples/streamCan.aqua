func toOpt(s: string) -> ?string:
  str: *string
  str <<- s
  <- str

func accumRes() -> *?string:
  res_accum: *?string
  a <- toOpt("a")
  res_accum <<- a
  res_accum <- toOpt("b")
  res_accum <<- nil
  <- res_accum
